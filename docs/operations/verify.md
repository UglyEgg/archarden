# Verify

`verify` checks whether the host listener/exposure model matches expectations.

## Purpose

- catch unexpected public listeners
- validate the designed trust boundaries
- provide a fast post-run sanity check

This is one of the more valuable parts of the project because it expresses intent: not merely “services run,” but “services are bound where they are supposed to be bound.”
