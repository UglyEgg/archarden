# Doctor

`doctor` is the read-only diagnostics command.

## What it covers

- service status
- listener bindings
- container health
- notification config gaps
- optionally a bundle for support/debugging

## Why it matters

A lot of personal infrastructure projects stop at install scripts. `doctor` is part of what makes Archarden read more like an operations artifact than a one-off bootstrap script.
