# CLI Reference

Primary commands:

- `apply`
- `verify`
- `doctor`
- `wg export`
- `lockdown`
- `creds`
- `notify init`

See the top-level README for the current authoritative flag list until a generated CLI reference is added.
